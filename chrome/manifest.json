{
  "manifest_version": 2,
  "name": "Remote browser",
  "description": "This extension is used to have remote control on browser with websockets",
  "version": "0.0.1",
  "background": {
    "scripts": ["scripts/background.js"]
  }
}
